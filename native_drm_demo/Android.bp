package {
    // See: http://go/android-license-faq
    // A large-scale-change added 'default_applicable_licenses' to import
    // all of the 'license_kinds' from "external_libdrm_license"
    // to get the below license kinds:
    //   SPDX-license-identifier-MIT
    default_applicable_licenses: ["external_libdrm_license"],
}

// build = ["Android.sources.bp"]

cc_binary {
    name: "drmhowto_modeset",
    defaults: [
        "libdrm_defaults",
    ],

	srcs: [
		"modeset.c",
	],

    shared_libs: ["libdrm"],
    static_libs: ["libdrm_util"],
    vendor_available: true,
}

cc_binary {
    name: "drmhowto_modeset_doublebuffer",
    defaults: [
        "libdrm_defaults",
    ],

	srcs: [
		"modeset-double-buffered.c",
	],

    shared_libs: ["libdrm"],
    static_libs: ["libdrm_util"],
    vendor_available: true,
}

cc_binary {
    name: "drmhowto_modeset_vsync",
    defaults: [
        "libdrm_defaults",
    ],

	srcs: [
		"modeset-vsync.c",
	],

    shared_libs: ["libdrm"],
    static_libs: ["libdrm_util"],
    vendor_available: true,
}

cc_binary {
    name: "drmhowto_modeset_atomic",
    defaults: [
        "libdrm_defaults",
    ],

	srcs: [
		"modeset-atomic.c",
	],

    shared_libs: ["libdrm"],
    static_libs: ["libdrm_util"],
    vendor_available: true,
}
